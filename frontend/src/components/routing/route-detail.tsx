import { Card } from "../ui/card";
import { travelModes, type BaseRoute } from "./types";

export function RouteDetail({ route }: { route: BaseRoute }) {
  return <section aria-labelledby="route-detail-title" className="grid gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.08em] text-[var(--color-primary-strong)]">Detail base route · {travelModes[route.profile]}</p><h2 id="route-detail-title" className="mt-1 font-[family-name:var(--font-display)] text-2xl font-bold text-[var(--color-ink)]">{route.origin} ke {route.destination}</h2></div><Card className="grid grid-cols-2 gap-3 p-4"><div><span className="block text-sm text-[var(--color-muted)]">Jarak</span><strong className="block text-xl text-[var(--color-ink)]">{route.distanceKm} km</strong></div><div><span className="block text-sm text-[var(--color-muted)]">Waktu perjalanan</span><strong className="block text-xl text-[var(--color-ink)]">± {route.estimatedMinutes} menit</strong></div></Card><ol className="relative grid gap-5 border-l-2 border-[var(--color-primary-soft)] pl-5">{route.steps.map((step, index) => <li key={step.id} className="relative"><span aria-hidden="true" className="absolute -left-[1.85rem] top-1 size-3 rounded-full bg-[var(--color-primary)] ring-4 ring-white" /><p className="font-semibold text-[var(--color-ink)]">{step.instruction}</p>{step.distanceMeters > 0 && <p className="mt-1 text-sm text-[var(--color-muted)]">{step.distanceMeters} m</p>}{index === 0 && <p className="mt-1 text-sm text-[var(--color-muted)]">Rute ORS; petunjuk belokan belum tersedia.</p>}</li>)}</ol></section>;
}
